import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cursoangular.app',
  appName: 'FirstAngularApp',
  webDir: 'dist/first-angular-app',
};

export default config;
